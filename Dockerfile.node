FROM node:latest

ARG PACKAGE_PATH=
ARG WORKING_DIR=

WORKDIR ${WORKING_DIR}

#COPY ${PACKAGE_PATH}/package.json ${PACKAGE_PATH}/yarn.lock ${WORKING_DIR}

RUN yarn

COPY ${PACKAGE_PATH} ${WORKING_DIR}

VOLUME $WORKING_DIR/node_modules

CMD [ "yarn", "start" ]
